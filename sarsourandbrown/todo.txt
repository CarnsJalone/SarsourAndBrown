1) Fix CSS in Contact page. The form needs to sit on the left side of the page. 
2) Fix CSS for the two links at the bottom of the page in base.html
3) Broke the contact form by adding a telephone verification, needs fixed